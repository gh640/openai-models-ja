[tool.poetry]
name = "openai-models-ja"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
openai = "^1.12.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
